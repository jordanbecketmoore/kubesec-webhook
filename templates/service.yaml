apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubsec-webhook-helm.fullname" . }}
  labels:
    {{- include "kubsec-webhook-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubsec-webhook-helm.selectorLabels" . | nindent 4 }}
