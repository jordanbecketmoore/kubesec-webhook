apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubsec-webhook-helm.fullname" . }}-test-connection"
  labels:
    {{- include "kubsec-webhook-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubsec-webhook-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
