apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: kubesec-server
  name: kubesec-server
spec:
  ports:
  - name: https
    port: 443
    protocol: TCP
    targetPort: https
  selector:
    app: kubesec-server
  type: ClusterIP
